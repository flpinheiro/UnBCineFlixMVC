@model UnBCineFlix.Models.MovieTheater

@{
    ViewData["Title"] = "Create";
}

<h2>Create</h2>

<h4>MovieTheater</h4>
<hr />
<p class="text-danger">@ViewData["erro"]</p>
<p> @Html.ValidationSummary(true)</p>
<div class="row">
    <div class="col-md-4">
        
        <form asp-action="Create">
            <div asp-validation-summary="ModelOnly" class="text-danger"></div>
            <div class="form-group">
                <label asp-for="MovieTheaterNumber" class="control-label"></label>
                <input asp-for="MovieTheaterNumber" class="form-control" />
                <span asp-validation-for="MovieTheaterNumber" class="text-danger"></span>
            </div>
            <div class="form-group">
                <label asp-for="QtdRow" class="control-label"></label>
                <input asp-for="QtdRow" class="form-control" />
                <span asp-validation-for="QtdRow" class="text-danger"></span>
            </div>
            <div class="form-group">
                <label asp-for="QtdCol" class="control-label"></label>
                <input asp-for="QtdCol" class="form-control" />
                <span asp-validation-for="QtdCol" class="text-danger"></span>
            </div>
            <div class="form-group">
                <label asp-for="AddressCompanyId" class="control-label"></label>
                <select asp-for="AddressCompanyId" class="form-control" asp-items="ViewBag.AddressCompanyId"></select>
            </div>
            <div class="form-group">
                <input type="submit" value="Create" class="btn btn-default" />
            </div>
        </form>
    </div>
</div>

<div>
    <a asp-action="Index">Back to List</a>
</div>

@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}
