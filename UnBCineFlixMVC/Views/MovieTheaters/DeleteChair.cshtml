@model UnBCineFlix.Models.Chair

@{
    ViewData["Title"] = "View";
}

<h2>View</h2>

<h3>Are you sure you want to delete this?</h3>
<div>
    <h4>Chair</h4>
    <hr />
    <dl class="dl-horizontal">
        <dt>
            @Html.DisplayNameFor(model => model.MovieTheater)
        </dt>
        <dd>
            @Html.DisplayFor(model => model.MovieTheater.AddressCompanyId)
        </dd>
    </dl>
    
    <form asp-action="DeleteChair">
        <input type="hidden" asp-for="Row" />
        <input type="hidden" asp-for="Col" />
        <input type="hidden" asp-for="AddressCompanyId" />
        <input type="hidden" asp-for="MovieTheaterNumber" />
        <input type="submit" value="Delete Chair" class="btn btn-default" /> |
        <a asp-action="Index">Back to List</a>
    </form>
</div>
