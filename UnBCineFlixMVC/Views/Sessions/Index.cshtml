@model IEnumerable<UnBCineFlix.Models.Session>

@{
    ViewData["Title"] = "Index";
}

<h2>Index</h2>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.MovieTheater.AddressCompany.Company.Name)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.MovieTheater.AddressCompany.Name)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.MovieTheaterNumber)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Movie)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.SessionTime)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.MovieTheater.AddressCompany.Company.Name)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.MovieTheater.AddressCompany.Name)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.MovieTheater.MovieTheaterNumber)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Movie.Title)
            </td>
            <td>
                @item.Movie.DurationToString()
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.Id">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.Id">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.Id">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
